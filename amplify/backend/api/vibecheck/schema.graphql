type Todo @model {
  id: ID!
  name: String!
  description: String
}

type User @model {
  id: ID!
  name: String!
  vibes: [ID]
}

# todo: add "moments"
type Vibe @model @key(fields: ["name"]) {
  id: ID!
  name: String!
  description: String!
  avatar: String
  contributorIds: [ID]
}
